package com.bm.wschat.feature.ticket.model;

public enum AssignmentStatus {
    PENDING,    // создано, но ещё не взято
    ACCEPTED,   // специалист нажал "Взять"
    REJECTED    // специалист отказался
}
